import { RemoteidMovement } from './remoteid-movement.model';

describe('RemoteidMovement', () => {
  it('should create an instance', () => {
    expect(new RemoteidMovement()).toBeTruthy();
  });
});
