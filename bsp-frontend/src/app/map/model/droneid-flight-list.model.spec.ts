import { DroneidFlightList } from './droneid-flight-list.model';

describe('DroneidFlightList', () => {
  it('should create an instance', () => {
    expect(new DroneidFlightList()).toBeTruthy();
  });
});
