import { RemoteidFlightList } from './remoteid-flight-list.model';

describe('RemoteidFlightList', () => {
  it('should create an instance', () => {
    expect(new RemoteidFlightList()).toBeTruthy();
  });
});
