import { Remoteid_movement_list } from './remoteid_movement_list.model';

describe('RemoteidsMovement', () => {
  it('should create an instance', () => {
    expect(new Remoteid_movement_list()).toBeTruthy();
  });
});
